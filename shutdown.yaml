- name: shutdown redhat instances
  hosts: all
  become: true

tasks:
  - name: shutdown only redhat instances
    ansible.builtin.command: /sbin/shutdown  -h now

    when:
      ansible_facts['os_family'] == 'redhat'